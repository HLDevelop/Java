public boolean sameEnds(int[] nums, int len) {
  boolean result = true;
  int n = len;
  
  for(int i=0;i<n;i++){
  if(!(nums[i]==nums[nums.length-n+i])){
  result = false;
  }
  }
  
  return result;
}
