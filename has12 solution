public boolean has12(int[] nums) {
  boolean ones = false;
  boolean twos = false;
  
  for(int i =0;i<nums.length;i++){
  if(nums[i]==1){
  ones = true;
  }
  if(nums[i]==2&&ones){
  twos = true;
  }
  }
  return twos;
}
